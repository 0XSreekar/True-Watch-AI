# Model weights are referenced by id and resolved at runtime. None are committed.
*.onnx
*.pt
*.pth
*.engine
*.bin
*.safetensors
# Detector downloads land here at boot (models/detector_weights.py).
cache/
