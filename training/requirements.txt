# Training toolchain for TRUEWATCH Phase 2 (train, evaluate, export, benchmark).
#
# LICENCE ISOLATION. Ultralytics is AGPL-3.0. It is imported only by files under training/ (train.py, _predict.py, export_onnx.py) and by
# nothing in edge/, backend/, frontend/ or datasets/. edge/ consumes an exported ONNX file through onnxruntime, so this dependency, and its
# licence obligations, stop at this folder. The repository itself is AGPL-3.0 (see training/README.md), which keeps the arrangement compliant.
#
# TORCH IS NOT LISTED. On Kaggle, torch and torchvision come from the GPU image, which ships a matched CUDA build; pip-installing another would
# replace it with a mismatched or CPU-only wheel. Locally, install torch and torchvision for your platform first, then this file.
#
# ultralytics is pinned to the exact version the smoke runs and tests were executed with (a resume must use the version that wrote the
# checkpoint, and train.py reads Ultralytics internals that change between releases). Everything else is a floor, not a pin.
#
# albumentations is optional. Ultralytics enables its built-in transforms when it can import it; train.py disables them either way so a
# Kaggle run (where it is installed) and a bare local run behave identically.

ultralytics==8.4.155
onnx>=1.17
onnxruntime>=1.20
onnxslim>=0.1.82
huggingface_hub>=0.30
pyyaml>=6.0
numpy>=1.26
opencv-python-headless>=4.8
pytest>=8.0
