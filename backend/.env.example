# TRUE WATCH — backend environment
# Copy to .env and adjust. Never commit the real .env.

NODE_ENV=development
PORT=4000

# Comma-separated list of origins allowed to call this API.
CORS_ORIGIN=http://localhost:5173

# --- Placeholders for the real system (not used by the demo yet) ---
# DATABASE_URL=postgres://user:pass@localhost:5432/truewatch
# JWT_SECRET=change-me
# JWT_EXPIRES_IN=12h
# VLM_ENDPOINT=http://localhost:8000/v1/judge
# EVIDENCE_STORE_PATH=./var/evidence
